listener {
    timeout = 300
    on-timeout = ~/.local/bin/matrix-idle.sh
    on-resume = kill $(cat /tmp/matrix-idle.pid 2>/dev/null) 2>/dev/null
}

listener {
    timeout = 600
    on-timeout = systemctl suspend
}
